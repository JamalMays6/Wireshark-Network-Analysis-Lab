# Captures

Placeholder — drop your saved `.pcapng` files here:

- `dns-lookup.pcapng` — the `nslookup google.com` capture, filtered `dns`
- `tcp-handshake.pcapng` — the `zero.webappsecurity.com` connection, filtered `tcp and ip.addr == 54.82.22.214`
- `http-post-credentials.pcapng` — the login form submission, filtered `http.request.method == POST`
- `tcp-stream.pcapng` — the capture used for the Follow TCP Stream exercise

Export each with **File → Export Specified Packets → Displayed** after applying the relevant filter, so each file only contains the traffic it's meant to demonstrate.
